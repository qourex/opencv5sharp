# Architecture

This document describes the internal architecture of OpenCV5Sharp.

## Overview

OpenCV5Sharp is a 3-layer binding system that bridges the gap between the native
OpenCV 5 C++ library and managed .NET code:

```
┌─────────────────────────────────────────────────────────────┐
│  .NET Application                                           │
│  (Your Code)                                                │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: Managed C# Wrappers                               │
│  ┌──────────┐  ┌─────────────┐  ┌────────────────┐         │
│  │ Classes  │  │    Cv2      │  │  NativeMethods │         │
│  │ (Mat,    │  │ (static    │  │  (P/Invoke     │         │
│  │  Net,   │  │  API like  │  │   declarations)│         │
│  │  ORB...)│  │  cv:: )    │  │                │         │
│  └──────────┘  └─────────────┘  └────────────────┘         │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: Native C++ Flat Exports (extern "C")              │
│  opencv5sharp_native.dll                                    │
│  - Flattens C++ classes to C functions                      │
│  - Translates cv::Ptr<T> to void*                           │
│  - Wraps all calls in try/catch                             │
├─────────────────────────────────────────────────────────────┤
│  Layer 1: OpenCV 5 C++ Library                              │
│  opencv_world500.dll + opencv_videoio_ffmpeg500_64.dll      │
│  - Official OpenCV binary distribution                      │
└─────────────────────────────────────────────────────────────┘
```

## Code Generation Pipeline

97% of the project's source code is automatically generated by `generator.py`.
The generation pipeline works as follows:

```
OpenCV C++ Headers (.hpp files)
         │
         ▼
┌──────────────────────┐
│  hdr_parser.py       │  (OpenCV's official Python header parser)
│  (from OpenCV repo)  │
└──────────┬───────────┘
           │  Parsed function/class/enum declarations
           ▼
┌────────────────────────┐
│  generator.py          │  (Our custom generator)
│                        │
│  Produces files:       │
│  ┌─────────────────────┐  │
│  │ Generated/          │──┼──► Module-split C# files (Classes.*, Cv2.*, etc.)
│  │ opencv5sharp_native │──┼──► C++ extern "C" flat exports (.cpp) and declarations (.h)
│  │   .cpp/.h           │  │
│  └─────────────────────┘  │
└────────────────────────┘
```

### How to Regenerate

```bash
# From workspace root:
py src/OpenCV5Sharp.Generator/generator.py --opencv-dir ./opencv --workspace-dir .
```

## Memory Ownership Model

### Rule 1: Objects returned by factory methods are OWNED by the caller

```csharp
// Caller MUST dispose:
using var mat = new Mat(100, 100, CvType.CV_8UC3);
using var orb = ORB.Create();
```

### Rule 2: DisposableOpenCVObject handles prevent double-free

The base class uses `Interlocked.Exchange` to atomically swap the handle to
`IntPtr.Zero` on dispose, ensuring exactly-once cleanup even under concurrent
disposal.

### Rule 3: Properties return copies, not internal references

Property getters that return objects (e.g., `Mat.Clone()`) return new allocations
that the caller must dispose.

## Error Propagation Model

```
OpenCV C++ throws cv::Exception
         │
         ▼
Native layer catches in try/catch block
Sets thread-local error state (_lastError, _lastErrorCode)
Returns error code (negative int)
         │
         ▼
Managed layer checks return value
Reads error message via opencv5sharp_getLastError()
Throws OpenCVException with message and code
```

## P/Invoke Conventions

- **Calling Convention**: `CallingConvention.Cdecl` (C standard, matches extern "C")
- **String Marshaling**: `UnmanagedType.LPUTF8Str` (UTF-8 on both sides)
- **Boolean Marshaling**: `UnmanagedType.U1` (C++ `bool` is 1 byte)
- **Struct Layout**: `LayoutKind.Sequential` for all blittable structs (Point, Size, Rect, Scalar, TermCriteria)
- **Object Handles**: `IntPtr` representing `void*` to heap-allocated C++ objects

## Directory Structure

```
OpenCV5/
├── src/
│   ├── OpenCV5Sharp/                 # Managed C# library (NuGet package)
│   │   ├── Generated/               # Generated: split module classes, enums, P/Invokes
│   │   ├── OpenCVException.cs       # Hand-written: custom exception
│   │   ├── AssemblyInfo.cs          # Hand-written: assembly attributes
│   │   ├── PlatformGuard.cs         # Hand-written: platform validation
│   │   └── runtimes/win-x64/native/ # Bundled native DLLs
│   ├── OpenCV5Sharp.Generator/      # Python code generator
│   │   ├── generator.py             # Main generator script
│   │   └── shadow_mat.hpp           # Shadow header for Mat extensions
│   └── OpenCV5Sharp.Native/         # C++ native wrapper
│       ├── opencv5sharp_native.cpp  # Generated: flat C exports
│       ├── opencv5sharp_native.h    # Generated: C export declarations
│       ├── CMakeLists.txt           # CMake build config
│       ├── compile.ps1              # PowerShell build script
│       └── build.bat                # Batch build script
├── tests/OpenCV5Sharp.Tests/        # Test suite
├── samples/OpenCV5Sharp.Samples/    # Example applications
├── opencv/                          # OpenCV 5 source (for header parsing)
└── opencv_prebuilt/                 # Pre-built OpenCV binaries
```

## Adding New OpenCV Modules

1. Add the module's header include to `opencv5sharp_native.h`
2. Add the header glob path to `generator.py`'s `parse_all()` method
3. If the module has a namespace, add it to `MODULE_NAMESPACES` dict
4. Run the generator: `py src/OpenCV5Sharp.Generator/generator.py`
5. Rebuild the native DLL and C# project
